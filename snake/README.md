# Snake

A small standalone browser Snake game with no build step or dependencies.

## Run

Open `snake/index.html` in a browser.

## Manual checks

- Start moving with arrow keys or `WASD`
- Eat food and confirm the score increments
- Pause with `Space` and resume with `Space` or the button
- Restart after a wall or self collision
